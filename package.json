{
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && npm run copy-runtime-assets",
    "copy-runtime-assets": "node scripts/copyRuntimeAssets.mjs",
    "check": "node --check game.js",
    "test": "vitest run"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "vitest": "latest"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  }
}

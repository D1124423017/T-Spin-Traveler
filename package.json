{
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "check": "node --check game.js",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "latest"
  }
}
